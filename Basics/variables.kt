fun main(){
    var num1 : Int = 10
    var num2 : Double ;
    num2 = 100.00;
    var num3 : Float = 100.00f
    var num4 : Long = 1000000000
    // var num5 ;
    var num6 : Byte = 1

    println("Your Int value is "+num1)
    println("The Double value is "+num2)
    // println("The Short value is "+num5)
}

// Val is used for immutable reference of the variables (Value cannot be changed or modified --> Constant or final)
// var is used for mutable reference of the variables (Value can be changed the modified)
// These variables can also be declared without the datatype when using var (same as python)
// But we should be giving them at the time of declaration if we are not assigning a value to it
// We can see the errors if we uncomment the above code